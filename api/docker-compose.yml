services:
  api:
    container_name: api-tp1
    build: .
    ports:
      - "3000:3000"

  postgres:
    container_name: postgres-tp1
    image: postgres:latest
    env_file:
        - .env
    ports:
      - "5432:5432"